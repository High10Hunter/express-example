name: hello world
on:
    push:
        paths:
            - '**.js'
        branches:
            - 'main'
    pull_request:
        types: [opened, synchronize, reopened]
        branches:
            - 'main'
jobs:
    say-hello:
        runs-on: ubuntu-latest
        steps:
            - run: echo "Hello world! Today is $TODAY"
