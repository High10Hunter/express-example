name: hello world
on:
    push:
        paths:
            - '**.js'
        branches:
            - 'main'
    pull_request:
        types: [synchronize]
        branches:
            - 'main'
jobs:
    say-hello:
        runs-on: ubuntu-latest
        steps:
            - uses: mheap/github-action-required-labels@v5
              with:
                  mode: exactly
                  count: 0
                  labels: 'do not build'
            - run: echo "Hello world"
