name: hello world
on:
    push:
        paths:
            - '**.js'

        branches:
            - 'main'
            - 'develop'
jobs:
    say-hello:
        runs-on: ubuntu-latest
        steps:
            - run: echo "Hello world! Today is $TODAY"
