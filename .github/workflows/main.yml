name: hello world
on:
    push:
        paths:
            - '**.js'
        branches:
            - 'main'
    pull_request:
        types: [opened, synchronize, labeled, unlabeled]
jobs:
    say-hello:
        runs-on: ubuntu-latest
        steps:
            - run: echo "Hello world"
